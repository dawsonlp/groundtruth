# Copy to this directory as .env and supply a cryptographically random local-only password.
# Keep .env mode 0600. Do not use an account-level environment file or commit .env.
POSTGRES_PASSWORD=

# Optional overrides; these defaults use the approved local port block.
DOMAINCATALOG_POSTGRES_PORT=25432
DOMAINCATALOG_API_PORT=28000
